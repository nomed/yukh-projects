name: Protected release preflight

on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact semantic version from the merged release PR
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-preflight-${{ inputs.version }}
  cancel-in-progress: false

jobs:
  would-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out exact candidate
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Use Node.js 24
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - name: Validate candidate without publication
        env:
          RELEASE_VERSION: ${{ inputs.version }}
          RELEASE_COMMIT: ${{ github.sha }}
          MCP_IMPLEMENTATION_COMMIT: a4f05f673bb0a03f66fc9864372cee7839ed78d1
        shell: bash
        run: |
          set -euo pipefail
          test "${GITHUB_REF}" = "refs/heads/main"
          npm ci
          npm test
          npm run verify:bundles
          npm audit --audit-level=moderate
          node scripts/create-release-sbom.mjs "$RELEASE_VERSION" "$MCP_IMPLEMENTATION_COMMIT" > release.spdx.json
          node scripts/create-release-package.mjs "$RELEASE_VERSION" release-package.tgz
          node scripts/release-provenance.mjs "$RELEASE_VERSION" "$MCP_IMPLEMENTATION_COMMIT" release-package.tgz > release-provenance.json
          node scripts/release-preflight.mjs "$RELEASE_VERSION" "$RELEASE_COMMIT" > would-publish.json
          mkdir release-assets
          cp RELEASE_NOTES.md "release-assets/yukh-projects-release-notes-${RELEASE_VERSION}.md"
          cp action.yml "release-assets/yukh-projects-action-${RELEASE_VERSION}.yml"
          cp apply-action.yml "release-assets/yukh-projects-apply-action-${RELEASE_VERSION}.yml"
          cp dist/action/index.js "release-assets/yukh-projects-action-${RELEASE_VERSION}.js"
          cp dist/apply/action.js "release-assets/yukh-projects-apply-action-${RELEASE_VERSION}.js"
          cp dist/apply/cli.js "release-assets/yukh-projects-apply-cli-${RELEASE_VERSION}.js"
          cp dist/apply/index.js "release-assets/yukh-projects-apply-library-${RELEASE_VERSION}.js"
          cp dist/apply/manifest.json "release-assets/yukh-projects-apply-manifest-${RELEASE_VERSION}.json"
          cp dist/cli/index.js "release-assets/yukh-projects-cli-${RELEASE_VERSION}.js"
          cp dist/mcp-effect-b/index.js "release-assets/yukh-projects-mcp-effect-b-${RELEASE_VERSION}.js"
          cp dist/mcp-effect-b/manifest.json "release-assets/yukh-projects-mcp-effect-b-manifest-${RELEASE_VERSION}.json"
          cp test/fixtures/mcp-effect-b-bridge-v2-vector.json "release-assets/yukh-projects-mcp-effect-b-bridge-v2-vector-${RELEASE_VERSION}.json"
          cp package-lock.json "release-assets/yukh-projects-lock-${RELEASE_VERSION}.json"
          cp release-package.tgz "release-assets/yukh-projects-package-${RELEASE_VERSION}.tgz"
          cp release-provenance.json "release-assets/yukh-projects-${RELEASE_VERSION}.provenance.json"
          cp release.spdx.json "release-assets/yukh-projects-${RELEASE_VERSION}.spdx.json"
          cp would-publish.json release-assets/would-publish.json
          (cd release-assets && sha256sum yukh-projects-* > "yukh-projects-${RELEASE_VERSION}.sha256")
      - name: Upload read-only qualification evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-qualification-${{ github.sha }}
          path: release-assets/
          if-no-files-found: error
          retention-days: 1
      - name: Assert no publication authority
        shell: bash
        run: |
          set -euo pipefail
          test "${ACTIONS_ID_TOKEN_REQUEST_URL:-}" = ""
          test "${ACTIONS_ID_TOKEN_REQUEST_TOKEN:-}" = ""
