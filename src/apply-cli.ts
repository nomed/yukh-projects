import { loadWorkspacePolicy, parseRuntimeScope } from "./runtime-input.js";
import { parseApplyCliArgs, readApprovalArtifact, readBoundedFd, readExclusiveWorkspaceFile } from "./apply-runtime-input.js";
import { runApplyEntrypoint } from "./apply-entrypoint.js";
import type { ApplyHostFactory } from "./apply-host.js";
import type { PublicApplyReport } from "./executor.js";

function failure(planId:string):PublicApplyReport{return{schema:1,status:"error",planId:/^[a-f0-9]{64}$/u.test(planId)?planId:"invalid",counts:{already_converged:0,verified:0,failed:0,not_attempted:0},remaining:0,diagnostics:[{code:"YKP-APPLY-001",severity:"error",message:"apply request is invalid"}]};}
export async function applyCliMain(argv:readonly string[],workspace:string,factory:ApplyHostFactory,write:(value:string)=>void):Promise<number>{let approvedPlanId="invalid";try{const options=parseApplyCliArgs(argv);approvedPlanId=options.approvedPlanId;const [readToken,writeToken,policySource,approvalArtifact,approvalPublicKey]=await Promise.all([readBoundedFd(options.readTokenFd),readBoundedFd(options.writeTokenFd),loadWorkspacePolicy(workspace,options.policyPath),readApprovalArtifact(workspace,options.approvalFile),readExclusiveWorkspaceFile(workspace,options.approvalPublicKeyFile)]);if(readToken===writeToken)throw new TypeError("credential profiles must be distinct");const requestedScope=parseRuntimeScope({owner:options.owner,repository:options.repository,projectNumber:options.projectNumber,issueNumber:options.issueNumber}),runtime=await factory.create({reconciliationMode:options.mode==="legacy-apply-v1"?"legacy-v1":"native-v1",requestedScope,policySource,readToken,writeToken}),report=await runApplyEntrypoint({approvedPlanId:options.approvedPlanId,protectedEnvironment:options.environment,scope:runtime.scope,approvalArtifact,approvalPublicKey},runtime.host),receipt=report.status==="deferred"?runtime.deferredReceipt?.():undefined;write(`${JSON.stringify({...report,...(receipt?{deferredReceipt:receipt}:{})})}\n`);return report.status==="success"?0:report.status==="deferred"?6:5;}catch{write(`${JSON.stringify(failure(approvedPlanId))}\n`);return 2;}}
